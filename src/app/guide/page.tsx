import { AppShell } from "@/components/AppShell";
import { DESIGN_PARTNER_PROTOCOL, TRANSFER_METHOD } from "@/lib/guidance";

export default function GuidePage() { return <AppShell><div className="max-w-3xl"><p className="text-sm text-emerald-300">承策使用方法</p><h1 className="mt-1 text-3xl font-semibold">不是把创始人写成 SOP，而是把关键判断交给组织。</h1><p className="mt-4 leading-7 text-slate-300">承策适合仍依赖创始人判断的 20–200 人团队。它以“证据—规则—演练—复审”为循环，避免把经验提炼变成脱离情境的口号。</p><section className="mt-8 space-y-4">{TRANSFER_METHOD.map((item, index) => <article className="card" key={item.key}><p className="text-xs text-emerald-300">第 {index + 1} 步</p><h2 className="mt-1 text-lg font-semibold">{item.title}</h2><p className="mt-2 leading-7 text-slate-400">{item.detail}</p></article>)}</section><section className="card mt-8"><h2 className="text-lg font-semibold">设计伙伴的 14 天验收</h2><p className="mt-2 text-slate-300">{DESIGN_PARTNER_PROTOCOL.outcome}</p><ul className="mt-4 list-disc space-y-2 pl-5 text-sm text-slate-400">{DESIGN_PARTNER_PROTOCOL.success.map((item) => <li key={item}>{item}</li>)}</ul></section></div></AppShell>; }
