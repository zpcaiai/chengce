export function CapabilityHeatmap({ capabilities }: { capabilities: { id: string; name: string; dependencyScore: number; repeatabilityScore: number }[] }) {
  if (!capabilities.length) return null;
  return <div className="card"><div className="flex items-end justify-between"><div><h2 className="font-semibold">创始人依赖热力图</h2><p className="mt-1 text-sm text-slate-400">右上角：越依赖创始人、越难复制，越应优先转移。</p></div><span className="text-xs text-slate-500">横轴：可复制性 →</span></div><div className="relative mt-5 grid h-64 grid-cols-4 grid-rows-4 overflow-hidden rounded-xl border border-slate-800 bg-slate-950/70">{[...Array(16)].map((_, index) => <div key={index} className="border border-slate-800/70"/>)}{capabilities.map((capability) => <div key={capability.id} title={`${capability.name} · 依赖 ${Math.round(capability.dependencyScore * 100)}% · 可复制 ${Math.round(capability.repeatabilityScore * 100)}%`} className="absolute max-w-28 -translate-x-1/2 translate-y-1/2 rounded-full border border-amber-300/60 bg-amber-950 px-2 py-1 text-center text-xs text-amber-100 shadow-lg" style={{ left: `${8 + capability.repeatabilityScore * 84}%`, bottom: `${8 + capability.dependencyScore * 84}%` }}>{capability.name}</div>)}</div><p className="mt-2 text-xs text-slate-500">纵轴：创始人依赖 →</p></div>;
}
